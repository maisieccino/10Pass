<UserControl
    x:Class="_10Pass.controls.MenuItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_10Pass.controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="300">

    <Button Width="300" Height="50" Background="{Binding BackgroundColor}" Foreground="{Binding ForegroundColor}" Padding="0">
    <StackPanel x:Name="root" Orientation="Horizontal" Height="50">
        <TextBlock Width="50" FontFamily="Segoe MDL2 Assets" Foreground="{Binding ForegroundColor}" x:Name="txtIcon" Text="{Binding Icon}" TextAlignment="Center" VerticalAlignment="Center" FontSize="29.333"/>
        <TextBlock Width="235" FontFamily="Segoe UI Light" Foreground="{Binding ForegroundColor}" x:Name="txtLabel" Text="{Binding Label}" TextAlignment="Left" FontSize="26.667" HorizontalAlignment="Right" Margin="15,0,0,0" LineHeight="29.333" VerticalAlignment="Center"/>
    </StackPanel>
    </Button>
</UserControl>
